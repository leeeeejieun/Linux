
---
- name: 로그 파일 수집 – fetch 모듈
  hosts: all
  tasks:
    - name: 로그 수집 - /var/log/secure -> secure-backups/ # 디렉터리가 없으면 자동 생성
      fetch:
        src: /var/log/secure
        dest: "secure-backups/{{ inventory_hostname }}-securelog"
        flat: yes
